module.exports = {

  ESCAPE_KEY: 27,

  stylePrefix: 'com-',

  defaults: [],

  // Editor model
  // 编辑模式
  em: null,

  // If true center new first-level components
  // 如果TRUE新的第一级组件
  firstCentered: true,

  // If true the new component will created with 'height', else 'min-height'
  // 如果TRUE，新的组件将创建'高度'，否则'分钟高度'
  newFixedH: false,

  // Minimum height (in px) of new component
  // 新组件的最小高度（PX）
  minComponentH: 50,

  // Minimum width (in px) of component on creation
  // 创建时组件的最小宽度（PX）
  minComponentW: 50,
};
